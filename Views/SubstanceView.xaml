<Window x:Class="PDSCalculatorDesktop.Views.SubstanceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Управление загрязняющими веществами" 
        Height="700" 
        Width="1050"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <materialDesign:Card Margin="20" 
                        Padding="25"
                        UniformCornerRadius="15"
                        materialDesign:ElevationAssist.Elevation="Dp8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" 
                       Orientation="Horizontal" 
                       Margin="0,0,0,20">
                <materialDesign:PackIcon Kind="Flask" 
                                        Width="32" 
                                        Height="32"
                                        VerticalAlignment="Center" 
                                        Foreground="#F57C00"/>
                <TextBlock Text="Управление загрязняющими веществами"
                          Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                          VerticalAlignment="Center"
                          Margin="12,0,0,0"
                          Foreground="#F57C00"/>
            </StackPanel>

            <materialDesign:Card Grid.Row="1" 
                                Padding="15"
                                Margin="0,0,0,20"
                                UniformCornerRadius="10"
                                Background="#FFF3E0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:PackIcon Grid.Column="0"
                                            Kind="Magnify"
                                            Width="24"
                                            Height="24"
                                            VerticalAlignment="Center"
                                            Margin="0,0,10,0"
                                            Foreground="#F57C00"/>

                    <TextBox Grid.Column="1"
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                            materialDesign:HintAssist.Hint="Поиск по коду или названию вещества"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                            FontSize="14"
                            VerticalContentAlignment="Center"/>
                </Grid>
            </materialDesign:Card>

            <materialDesign:Card Grid.Row="2"
                                Margin="0,0,0,20"
                                UniformCornerRadius="10">
                <DataGrid ItemsSource="{Binding Substances}"
                         SelectedItem="{Binding SelectedSubstance}"
                         AutoGenerateColumns="False"
                         IsReadOnly="True"
                         SelectionMode="Single"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         CanUserResizeRows="False"
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column"
                         RowBackground="White"
                         FontSize="13"
                         materialDesign:DataGridAssist.CellPadding="10 8 10 8"
                         materialDesign:DataGridAssist.ColumnHeaderPadding="20 12 10 12"
                         MouseLeftButtonDown="DataGrid_MouseLeftButtonDown">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Foreground" Value="#F57C00"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                            <Setter Property="Height" Value="45"/>
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderThickness" Value="4,0,0,0"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFE0B2"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FFF3E0"/>
                                    <Setter Property="BorderBrush" Value="#F57C00"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Код вещества"
                                            Binding="{Binding Code}" 
                                            Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Название вещества" 
                                           Binding="{Binding Name}" 
                                           Width="*"/>

                        <DataGridTextColumn Header="КНК" 
                                           Binding="{Binding KNK, StringFormat=N3}" 
                                           Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <StackPanel Grid.Row="3" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Left">

                <Button Command="{Binding AddCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Width="140"
                       Height="40"
                       Margin="0,0,12,0"
                       Background="#F57C00"
                       BorderBrush="#F57C00"
                       materialDesign:ButtonAssist.CornerRadius="10"
                       materialDesign:ElevationAssist.Elevation="Dp4"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBooleanConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Добавить" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding EditCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Width="140"
                       Height="40"
                       Margin="0,0,12,0"
                       Background="#2196F3"
                       BorderBrush="#2196F3"
                       materialDesign:ButtonAssist.CornerRadius="10"
                       materialDesign:ElevationAssist.Elevation="Dp4"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBooleanConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Pencil" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Изменить" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding DeleteCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Width="140"
                       Height="40"
                       Margin="0,0,12,0"
                       Background="#D32F2F"
                       BorderBrush="#D32F2F"
                       materialDesign:ButtonAssist.CornerRadius="10"
                       materialDesign:ElevationAssist.Elevation="Dp4"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBooleanConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Delete" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Удалить" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding RefreshCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Width="140"
                       Height="40"
                       Background="#616161"
                       BorderBrush="#616161"
                       materialDesign:ButtonAssist.CornerRadius="10"
                       materialDesign:ElevationAssist.Elevation="Dp4"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InvertBooleanConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="Обновить" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="Medium"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>